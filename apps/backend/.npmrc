# Same reason as the repository root, and npm does not inherit that one: it
# reads .npmrc from the directory it runs in, not from parent folders.
#
# Here the mismatch is ts-jest@28 asking for jest ^28 against the jest 27 that
# is installed. Nothing runs those tests today, and installing fails outright
# without this.
legacy-peer-deps=true
