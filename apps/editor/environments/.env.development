# The editor writes, so it always talks to the local Node backend: the PHP
# gateway in production is read-only by construction.
VITE_FOO=backend
VITE_BACKEND_URL=http://localhost:5000/api
VITE_DATABASE=front.sqlite3.png
VITE_DEFAULT_LANG=en
VITE_LANGS=en,es,fr,pt,de,el,it
VITE_STALE_TIME=0
VITE_COOKIE_DAYS=7
