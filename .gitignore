# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.js

#vscode
/.vscode
/.vs
/.claude

# testing
/coverage

# production
/build

# Deploy credentials. Never commit: this repository is public.
.env.deploy

# The backend's own environment: the path to the database and the port it
# listens on. It was tracked, in a public repository, carrying a secret that
# nothing read. apps/backend/.env.example lists the keys.
apps/backend/.env
.deploy-manifest.json

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Piece flags downloaded from Wikipedia and their raster sizes: 395 MB that the
# editor regenerates, so they are deployed but not versioned.
/data/customFlags
/data/shapefiles
